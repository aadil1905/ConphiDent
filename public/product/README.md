# Public product media

Add only sanitized product screenshots and recordings here. Never include real patient data, phone numbers, clinic details, credentials, tenant IDs, tokens, or real WhatsApp conversations.

Folders: `dashboard`, `patients`, `appointments`, `clinical`, `billing`, `whatsapp`, `crm`, `laboratory`, `inventory`, `reports`, and `videos`.

Use native video controls, a poster image, and `preload="metadata"` when a real sanitized recording is added.
