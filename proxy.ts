import { NextResponse, type NextRequest } from "next/server";
import { readSessionCookie } from "@/lib/auth-token";
const publicApi = ["/api/webhook", "/api/health", "/api/cron/booking-reminders", "/api/cron/follow-ups", "/api/cron/whatsapp-outbox", "/api/public-intake", "/api/demo-requests"];
export async function proxy(request: NextRequest) { const { pathname } = request.nextUrl; const host = (request.headers.get("host") || "").split(":")[0].toLowerCase(); const setupHost = (process.env.SETUP_DOMAIN || "").split(":")[0].toLowerCase(); const local = host === "localhost" || host === "127.0.0.1"; const platformDomain = (process.env.PLATFORM_DOMAIN || "").split(":")[0].toLowerCase(); const isTenantHost = Boolean(platformDomain && host.endsWith(`.${platformDomain}`) && host !== setupHost); if (setupHost && host === setupHost && (pathname === "/" || pathname === "/platform" || pathname.startsWith("/dashboard"))) return NextResponse.redirect(new URL("/setup", request.url)); if ((pathname.startsWith("/setup") || pathname.startsWith("/platform")) && !local && (host !== setupHost || isTenantHost)) return NextResponse.redirect(new URL("/dashboard", request.url)); if (publicApi.some((path) => pathname.startsWith(path))) return NextResponse.next(); const session = await readSessionCookie(request.cookies.get("dentalai_session")?.value); if (session) return NextResponse.next(); if (pathname.startsWith("/api/")) return NextResponse.json({ error: "Authentication required." }, { status: 401 }); return NextResponse.redirect(new URL("/login", request.url)); }
export const config = { matcher: ["/", "/setup/:path*", "/platform/:path*", "/dashboard/:path*", "/settings/:path*", "/api/:path*"] };
